# To add shadow

- Use `text-shadow: [horizontal offset] [vertical offset] [optional: blur]` \
- Increase in horizontal offset -> text moves to right \
- Increase in vertical offset -> text moves downwards \
- Blur: to control the blurriness

- Similary `box-shadow` can also be used