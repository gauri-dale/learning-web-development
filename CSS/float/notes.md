# Float
Allows other elements to flow around it.

- `float: left` allows the image to float around the left part of the screen and the text wraps around the image on the right.

- similarly `float: right`