# Navigation Bar
- `<nav></nav>` is used for a navigation bar.
- between this <nav> tag, `<ul>` and `<li>` elements allow users to easily navigate between different sections or pages of a site. \

- `<main></main>`: The main element represents the main content of the body of a document or application. 