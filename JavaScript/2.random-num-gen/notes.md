# This generates a random number between 0 and 1
`Math.random()` (includes decimals)

# If we want a random number between a particular range
- For example, between 1 and 6
`(Math.random() * 6) +1` (this includes decimals)

- Without decimals `Math.floor(Math.random() * 6);`